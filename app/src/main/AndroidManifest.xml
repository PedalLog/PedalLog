<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> <!-- For network provider location -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> <!-- For GPS and network provider location -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" /> <!-- To access location when app is minimized -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:name=".PedalLogApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PedalLog"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".ui.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.SetupActivity"
            android:exported="false" />
        <activity
            android:name=".ui.TrackingActivity"
            android:exported="false" />
        <activity
            android:name=".ui.PedalLogActivity"
            android:exported="false"
            android:launchMode="singleTask">
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:label="@string/app_settings"
            android:exported="true">
        </activity>

        <activity
            android:name=".ui.JourneyMapActivity"
            android:exported="false" />

        <service android:name=".services.TrackingService"
                 android:exported="false"
                 android:foregroundServiceType="location" />
    </application>

</manifest>
